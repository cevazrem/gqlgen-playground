apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus   # 👈 фиксированный идентификатор, важная строчка
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true